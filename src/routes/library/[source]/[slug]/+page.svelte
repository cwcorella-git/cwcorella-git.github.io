<script lang="ts">
	import LibraryView from '$lib/components/library/LibraryView.svelte';
</script>

<!-- A document's own address. The same page as /library: LibraryView reads the
     path and opens the document over the list. -->
<LibraryView />
